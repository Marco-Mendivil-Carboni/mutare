\documentclass[a4paper,10pt,twocolumn]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

\usepackage[backend=biber]{biblatex}
\addbibresource{mutare.bib}

\usepackage{lmodern}

\usepackage[margin=2cm]{geometry}

\usepackage{mathtools,amssymb}

\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{calc, positioning}

\usepackage{booktabs}
\usepackage{subcaption}

\usepackage[hidelinks,pdfusetitle]{hyperref}

\title{Evolutionary adaptation through bet-hedging in finite populations under fluctuating environments}
\author{Marco Mendívil Carboni}

\raggedbottom

\begin{document}

\maketitle

\begin{abstract}
    Natural populations rarely evolve under stable conditions and without limitations: environments fluctuate unpredictably and resources constrain population size. In this work, we introduce a minimal stochastic model to study how evolutionary adaptation unfolds under these conditions. The model consists of a population undergoing a birth--death process in a randomly switching environment, where individuals carry heritable phenotypic strategies that determine the distribution of phenotypes across generations.

    By first analyzing fixed strategies, we show that environmental variability can favor phenotypic diversity through bet-hedging, and that strategies maximizing mean growth rate generally differ from those minimizing extinction risk. When evolution is enabled, populations do not simply maximize growth, but instead converge to intermediate strategies that balance reproductive performance and survival. This trade-off becomes particularly relevant in finite populations, where extinction risk acts as an additional selective pressure.

    We further demonstrate that population size controls the relative importance of these effects: large populations recover the classical growth-maximizing behavior, while smaller populations evolve toward safer, more resilient strategies. A simple heuristic approximation, constructed from fixed-strategy observables, captures the main features of the evolutionary outcome and clarifies the competing roles of growth and extinction.

    Overall, our results provide a clear picture of how environmental fluctuations and demographic constraints jointly shape evolutionary adaptation, highlighting the central role of extinction risk in determining evolutionary outcomes in finite populations.
\end{abstract}

\section{Introduction and objectives} \label{sec:introduction}

Adaptation in fluctuating environments is a central problem in evolutionary biology \autocite{kingscollegeRiskUncertaintyEvolutionary1982}. Natural populations rarely experience constant conditions: rather, they face environments that change \emph{unpredictably} over time, often on ecological or evolutionary timescales. In such contexts, if sensing the changes in the environment is infeasible or too costly, it is well established in the ecology and population genetics literature that bet-hedging (i.e. investing only a fraction of the population to each possible phenotype) offers a good alternative to adapt to this fluctuating environment \autocite{kussellPhenotypicDiversityPopulation2005}.

In these circumstances our current understanding of evolutionary adaptation suggests that populations tend to evolve toward the strategy which maximizes their mean growth rate \autocite{gillespieNaturalSelectionVariances1977} \footnote{More precisely, \autocite{gillespieNaturalSelectionVariances1977} argues that what gets maximized is the geometric mean of the offspring number $\mu'$ (where $N(t+\Delta t)=\mu'\cdot N(t)$) but this is equivalent to maximizing the arithmetic mean of the growth rate $\langle\mu\rangle$ as we have defined it in \autoref{eq:avg_growth_rate}.}
\begin{equation} \label{eq:avg_growth_rate}
    \langle\mu\rangle=\left\langle\frac{1}{N(t)}\frac{dN}{dt}(t)\right\rangle.
\end{equation}
However, this is not an exact result for finite populations, and when environmental fluctuations are strong and extinction risk is non-negligible like in limited-size populations we expect to see deviations of this principle. In particular, we want to investigate whether natural selection in this conditions may favor strategies that sacrifice some average growth rate in exchange for a lower extinction rate $r_\text{ext}$.

Thus, the purpose of this work is to investigate, through numerical simulations, how the adaptation of finite populations in uncertain environments gives rise to phenotypic diversity and understand to what extent the strategies that emerge correspond to those of maximal growth or to safer, more resilient alternatives.

\section{The model: a Markov process} \label{sec:model}

\begin{figure}
    \centering
    \caption{Schematic diagram of our model: the \emph{environment} variable follows a simple markov process between two states (top), the population follows a \emph{birth-death process} with rates that depend on the environment and the phenotypes of the agents (middle) and finally, the phenotype of the offspring will be determined by the strategy of the parent and the offspring will inherit that same strategy unless a mutation occurs (bottom).} \label{fig:model_diagram}
    \begin{subfigure}{\linewidth}
        \centering
        \begin{tikzpicture}
            \tikzset{
                environment/.style={draw, rounded corners, minimum size=0.75cm, fill=lightgray!25},
                rate/.style={->, >=stealth, thick},
                node distance=2.25cm
            }
            \node[environment] (e1) {$1$};
            \node[environment, right of=e1] (e2) {$2$};
            \draw[rate, bend left=20] (e1) to node[above] {$\omega_t(1,2)$} (e2);
            \draw[rate, bend left=20] (e2) to node[below] {$\omega_t(2,1)$} (e1);
        \end{tikzpicture}
        \caption{Environment dynamics}
    \end{subfigure}
    \begin{subfigure}{\linewidth}
        \centering
        \begin{tikzpicture}
            \tikzset{
                agent/.style={draw, rounded corners, minimum height=0.75cm, minimum width=1.25cm, fill=lightgray!25},
                rate/.style={->, >=stealth, thick},
                node distance=2.75cm
            }
            \node[agent] (agent) {$\phi$, $s$};
            \node[agent, right of=agent, yshift=+0.5cm] (death) {$\varnothing$};
            \node[agent, right of=agent, yshift=-0.5cm] (agent_copy) {$\phi$, $s$};
            \node[right=0cm of agent_copy, draw=none] (plus) {$+$};
            \node[agent, right=0cm of plus] (offspring) {$\phi_o$, $s_o$};
            \draw[rate, bend left=10] (agent) to node[sloped, midway, above] {$\omega_d(e,\phi)$} (death);
            \draw[rate, bend right=10] (agent) to node[sloped, midway, below] {$\omega_b(e,\phi)$} (agent_copy);
        \end{tikzpicture}
        \caption{Birth-death process}
    \end{subfigure}
    \begin{subfigure}{\linewidth}
        \centering
        \begin{tikzpicture}
            \tikzset{
                trait/.style={},
                prob/.style={->},
                node distance=2.5cm
            }
            \node[trait] (phi_o) {$\phi_o=$};
            \node[trait, right of=phi_o, yshift=+0.5cm] (A) {$A$};
            \node[trait, right of=phi_o, yshift=-0.5cm] (B) {$B$};
            \draw[prob] (phi_o) -- node[sloped, midway, above] {$s(A)$} (A);
            \draw[prob] (phi_o) -- node[sloped, midway, below] {$s(B)$} (B);
            \node[trait, right of=phi_o, xshift=1.5cm] (s_o) {$s_o=$};
            \node[trait, right of=s_o, yshift=+0.5cm] (s) {$s$};
            \node[trait, right of=s_o, yshift=-0.5cm] (s_mut) {$s_\text{mut}$};
            \draw[prob] (s_o) -- node[sloped, midway, above] {$1-p_\text{mut}$} (s);
            \draw[prob] (s_o) -- node[sloped, midway, below] {$p_\text{mut}$} (s_mut);
        \end{tikzpicture}
        \caption{Phenotype and strategy of the offspring}
    \end{subfigure}
\end{figure}

To adress the questions outlined in the introduction we introduce the simple agent-based model illustrated in \autoref{fig:model_diagram}. The state of the system consists of only two components: the \emph{environment} and the \emph{population}.

The \emph{environment} is simply a discrete variable with two possible values \footnote{Of course this is easily generalizable to the case of $n_\text{env}$ different environments.}, $e\in\{1,2\}$, which evolves according to a continuous-time Markov chain with transition rates $\omega_t(e,e')$ between states $e$ and $e'$ \footnote{The possibility that this environment variable follows non-Markovian dynamics or takes continuous values is outside of the scope of this work.}.

The \emph{population} is made up of $N$ agents, each one of which will be characterized just by two traits: a \emph{phenotype} $\phi\in\{A,B\}$ \footnote{Again this could easily be generalized to more than two phenotypes.} and a \emph{phenotypic strategy} $s(\phi)$, which represents a probability distribution over the possible phenotypes \footnote{Since we restrict ourselves to two phenotypes, the strategy is fully specified by a single parameter, $s(A)$, as $s(B)=1-s(A)$. Throughout this work, we will therefore specify only this first component.}. The phenotype will determine the response of the agent to the environment while the phenotypic strategy will determine the phenotype of the offsprings of that agent, introducing a mechanism for phenotypic diversity to appear and be mantained \emph{across generations}.

In particular the population will follow a stochastic birth-death process in which the birth and death rates ($\omega_b(e,\phi)$ and $\omega_d(e,\phi)$ respectively) depend explicitly on both the current environment and the phenotype of each agent. Therefore, the behaviour of the population as a whole in a given environment will depend on its phenotypic composition.

Additionally, when an agent replicates, its offspring will generally inherit the parent's phenotypic strategy $s(\phi)$, but with probability $p_\text{mut}$ a mutation will occur, leading to a new random strategy \footnote{Thus, for simplicity, mutations in this model are assumed to be abrupt, even though this may not be the most common situation in biological systems. In \autoref{sec:incremental_mutations}, we extend the model by introducing an additional parameter to allow for incremental mutations, and show that this modification leads to qualitatively similar results, with only minor quantitative differences.}. This mechanism enables the evolutionary exploration of the space of possible phenotypic strategies, which constitute the heritable traits subject to selection, and therefore play the role of the \emph{genotype}.

Finally, as we pointed out in the introduction we expect to see deviations from the already known principles of evolution when the population size does not grow indefinitely. Therefore, we will limit the population size to its initial value, $N_\text{ini}$: whenever replication would cause the population to exceed this value, a randomly chosen agent is simultaneously removed. This mechanism mimics continuous-culture evolution experiments \autocite{collectiveEconomicPrinciplesCell}, and can be interpreted as imposing an effective ecological carrying capacity.

Despite the minimalism of the model, it is definitely impossible to solve it analytically. Therefore, we will perform simulations using the Gillespie algorithm \autocite{toralStochasticNumericalMethods} \footnote{More details about the simulations and the algorithm can be found in \autoref{sec:gillespie_algorithm}}. Still, as we will see in the next sections, the model is simple enough that its behavior can be readily analyzed, and at the same time it contains all the necessary elements to provide insight into the questions that motivate this work.

\section{Behaviour of the model without evolution} \label{sec:no_evolution}

We begin by solving the model without evolution, i.e. with $p_\text{mut}=0$ and assuming that all agents initially share the same strategy, $s_\text{ini}$. This kind of simulations (which we will call \texttt{fixed}) allow us to study the properties of individual strategies in isolation and, in turn, better understand the evolutionary outcomes that follow.

\begin{table}
    \caption{Set of parameters used in the simulations, which we will refer to as the ``assymetric parameters''. They represent a common biological situation in which there is a ``resistant'' phenotype and a ``persistent'' one.} \label{tab:assymetric_parameters}
    \begin{tabular}{c c c}
        \toprule
        symbol             & name                     & value                                                        \\
        \midrule
        $\omega_t(e,e')$   & transition rates         & $\begin{psmallmatrix}-1.0&+1.0\\+1.0&-1.0\end{psmallmatrix}$ \\
        \addlinespace
        $\omega_b(e,\phi)$ & birth rates              & $\begin{psmallmatrix}1.0&0.2\\0.0&0.0\end{psmallmatrix}$     \\
        \addlinespace
        $\omega_d(e,\phi)$ & death rates              & $\begin{psmallmatrix}0.0&0.0\\1.0&0.1\end{psmallmatrix}$     \\
        \addlinespace
        $p_\text{mut}$     & mutation probability     & $0.001$                                                      \\
        \addlinespace
        $N_\text{ini}$     & initial number of agents & $100$                                                        \\
        \bottomrule
    \end{tabular}
\end{table}

To proceed, we must first choose a specific set of model parameters. We start with the set shown in \autoref{tab:assymetric_parameters} \footnote{A completely different set of parameters is considered in \autoref{sec:symmetric_parameters}, which leads to the same main conclusions despite some differences.}. These parameters represent a common biological scenario in which there is a “resistant” phenotype and a “persistent” one \autocite{huftonPhenotypicSwitchingPopulations2018}. More precisely the meaning of this parameters is the following: both environments are equally likely and there is a transition rate between them of $1.0$; environment $1$ represents a ``favorable'' environment in which phenotype $A$ (the ``resistant'' one) replicates with a rate of $1.0$ while phenotype $B$ (the ``persistent'' one) does it with a rate of $0.2$, and neither of them die; environment $2$ instead represents ``harsh'' conditions in which phenotype $A$ dies with a rate of $1.0$ and phenotype $B$ with a rate of $0.1$, and neither of them are able to duplicate.

\begin{figure}
    \centering
    \caption{Mean growth rate (top) and extinction rate (bottom) as a function of the initial phenotypic strategy for \texttt{fixed} simulations with the parameter set in \autoref{tab:assymetric_parameters}. The strategy that maximizes mean growth (marked with a dashed line) differs from the one that minimizes extinction (marked with a dotted line), revealing a clear trade-off between these two possible objectives.} \label{fig:main_observables}
    \includegraphics{../sims/asymmetric/plots/strat_phe_0_i/avg_growth_rate.pdf}
    \includegraphics{../sims/asymmetric/plots/strat_phe_0_i/extinct_rate.pdf}
\end{figure}

\autoref{fig:main_observables} shows the mean growth rate $\langle\mu\rangle$ and the extinction rate $r_e$ measured in fixed-strategy simulations for this parameter set. The first important observation to make is that the maximum of $\langle\mu\rangle$ occurs neither at $s(A)=0$ nor at $s(A)=1$, indicating that in cases such as this one phenotypic diversity (that is, maintaining a mixture of both phenotypes) is the ``optimal'' strategy, a result already reported multiple times \autocite{xueBetHedgingDemographic2017}. As we will see, this is precisely the strategy reached by evolution at large $N_\text{ini}$. This behavior does not arise in all cases, but only when environmental fluctuations make each phenotype individually less favorable in the long term than their combination.

Secondly, we observe that the growth-maximizing strategy differs from the extinction-minimizing one \footnote{Again, this occurs for other parameter sets, as discussed in \autoref{sec:symmetric_parameters}.}. In particular, $\langle\mu\rangle$ is maximized at $s(A)\sim0.8$, while $r_\text{ext}$ is minimized at $s(A)\sim0.0$. As we will show later, this trade-off (and more precisely the different scaling of these two quantities with the population limit) plays a key role in shaping the evolutionary outcome.

We do not explore in detail how these results depend on the specific choice of parameters, as this has been extensively studied in previous work. Instead, we focus in the next section on the outcome of the evolutionary dynamics and on which strategies are ultimately selected under these conditions.

\section{Outcome of the evolutionary dynamics} \label{sec:evol_outcome}

\begin{figure}
    \centering
    \caption{Evolutionary dynamics in the ``asymmetric'' parameter set. At each time, we compute the population-averaged strategy $\overline{s}(A)$, that is, the average of $s(A)$ over all agents. Top: distribution of $\overline{s}(A)$ over time. Bottom: time average of $\overline{s}(A)$ (i.e., the mean of the distribution shown above). Results are shown for both \texttt{evol} (single initial strategy) and \texttt{evol(r)} (random initial strategies) simulations, illustrating the weak dependence on initial conditions. The dashed line indicates the strategy that maximizes mean growth, while the dotted line marks the one that minimizes extinction. The evolved strategy lies between these two, reflecting a compromise between growth and survival.} \label{fig:evol_results}
    \includegraphics{../sims/asymmetric/plots/strat_phe_0_i/dist_avg_strat_phe_0.pdf}
    \includegraphics{../sims/asymmetric/plots/strat_phe_0_i/avg_avg_strat_phe_0.pdf}
\end{figure}

We now enable evolution by allowing strategies to mutate and compete. We consider two types of simulations: in \texttt{evol} simulations, the population is initially composed of agents with the same strategy $s(A)_\text{ini}$, while in \texttt{evol(r)} simulations, the initial strategy of each agent is completely random.

To characterize the outcome of the evolutionary process, at each time we compute the population-averaged strategy $\overline{s}(A)$. In \autoref{fig:evol_results} we show the distribution of $\overline{s}(A)$ over time (top panel) and its time average (bottom panel). In both types of simulations, the system consistently converges toward a well-defined stationary distribution of $\overline{s}(A)$, with only a weak dependence on the initial condition.

A key observation is that the instantaneous dispersion of strategies $\sigma_s$ (defined as the spread of strategies present in the population at a given time) remains small. This dispersion is represented in the bottom panel of \autoref{fig:evol_results} as an error band, which is barely visible. At any given time, the population is therefore effectively characterized by a single dominant strategy. Over longer times, however, mutation-driven exploration causes this dominant strategy to drift through the space of possible strategies.

More importantly, we observe that the average evolved strategy lies between the growth-maximizing and extinction-minimizing strategies identified in the fixed-strategy simulations. As we will see in later sections, this behavior is not specific to this parameter set. This shows that evolution in finite populations does not optimize instantaneous growth alone, but instead selects strategies that represent a compromise between growth and extinction avoidance.

\begin{figure}
    \centering
    \caption{Dependence of the average evolved strategy on the population size $N_\text{ini}$ (top) and the mutation probability $p_\text{mut}$ (bottom). Increasing $N_\text{ini}$ drives the system toward the growth-maximizing strategy, while smaller populations favor safer strategies with lower extinction rates.} \label{fig:dependence_on_N_ini_and_p_mut}
    \includegraphics{../sims/asymmetric/plots/n_agents_i/avg_avg_strat_phe_0.pdf}
    \includegraphics{../sims/asymmetric/plots/prob_mut/avg_avg_strat_phe_0.pdf}
\end{figure}

Next, in \autoref{fig:dependence_on_N_ini_and_p_mut} we explore how the evolutionary outcome (namely, the average strategy $\langle\overline{s}\rangle(A)$) depends on the population size $N_\text{ini}$ and the mutation probability $p_\text{mut}$.

We first observe a strong dependence on $N_\text{ini}$. As anticipated, when $N_\text{ini}$ increases, $\langle \overline{s} \rangle(A)$ approaches the growth-maximizing value identified in \autoref{sec:no_evolution}. Only for small populations does the evolutionary outcome favor safer strategies with lower extinction rates.

In contrast, the dependence on the mutation probability is relatively weak, as $p_\text{mut}$ mainly controls the exploration of the strategy space \footnote{In \autoref{sec:incremental_mutations} we further examine the effect of changing the characteristics of mutations.}. For very large values of $p_\text{mut}$, $\sigma_s$ increases, indicating that the population no longer shares a common strategy at a given time. In fact, in the limit $p_\text{mut} \to 1$, the population behaves as if $s(A) = 0.5$, since strategies change randomly with every replication. Conversely, in the limit $p_\text{mut} \to 0$, mutations are absent, and the outcome is determined solely by selection among the strategies present in the initial condition.

\section{Heuristic interpretation of the evolutionary outcome} \label{sec:heuristic_interpretation}

\begin{figure}
    \centering
    \caption{Extinction rate $r_\text{ext}$ versus mean growth rate $\langle \mu \rangle$ for ``fixed'' strategies, defining a Pareto front that captures the trade-off between growth and survival. When evolution is enabled, the system converges to a point with a lower growth rate than the growth-maximizing strategy, but also with a reduced extinction rate.} \label{fig:pareto_front}
    \includegraphics{../sims/asymmetric/plots/strat_phe_0_i/rates.pdf}
\end{figure}

To better understand the outcome of the evolutionary process, it is useful to examine in more detail the trade-off between the mean growth rate and the extinction rate. As discussed in previous sections, these two quantities cannot be optimized simultaneously. If we plot, as in \autoref{fig:pareto_front}, the values of $r_\text{ext}$ against $\langle \mu \rangle$ for \texttt{fixed} strategies across the full strategy space, we obtain a curve known as a Pareto front \autocite{dinisPhaseTransitionsOptimal2020}. This curve represents the set of optimal trade-offs: any increase in growth necessarily comes at the cost of a higher extinction risk, and vice versa. In more general settings with more than two phenotypes, strategies would span a higher-dimensional space, and the Pareto front would correspond to the boundary (also known as the efficient frontier) of the accessible region.

In the same plot we also display the outcome obtained when evolution is enabled, allowing us to compare directly the resulting growth and extinction rates rather than the underlying strategy. We find that the system converges, with only a moderate dependence on the initial conditions, toward a point with a lower growth rate than the growth-maximizing strategy, but also with a reduced extinction rate.

Importantly, this point does not lie exactly on the Pareto front defined by fixed strategies, indicating that the evolutionary outcome cannot be associated with any single fixed strategy. Instead, both the mean growth rate and the extinction rate are well approximated by averages over the distribution of strategies:
\begin{equation} \label{eq:evol_averages}
    \langle\mu\rangle\approx\int\langle\mu\rangle(\overline{s})p(\overline{s})d\overline{s}, \quad r_\text{ext}\approx\int r_\text{ext}(\overline{s})p(\overline{s})d\overline{s}.
\end{equation}
Additional evidence supporting this interpretation is provided in \autoref{sec:supplementary_results}.

This behavior can be understood from the dynamical nature of the evolutionary process: at any given time, the population is effectively characterized by a single dominant strategy, but this strategy slowly changes due to mutations and reinitializations following extinction events. Over long times, the system can therefore be interpreted as a concatenation of fixed-strategy dynamics with different values of $\overline{s}$.

\begin{figure*}
    \centering
    \caption{Top panels: fixed-strategy results used to construct the heuristic approximation. The mean growth rate depends only weakly on $N_\text{ini}$, whereas the extinction rate follows an approximate power-law scaling. Bottom panels: expected average strategy from the heuristic distribution $\langle\overline{s}\rangle(A)^\ast$ and observed average strategy from simulations for different values of $N_\text{ini}$ and $p_\text{mut}$. Dashed lines indicate spline interpolations of the simulation data. The heuristic approximation captures the qualitative dependence of the evolutionary outcome on $N_\text{ini}$ and $p_\text{mut}$, albeit with quantitative discrepancies.}
    \label{fig:fixed_results_and_heuristic_approx}
    \includegraphics{../sims/asymmetric/plots/fixed/avg_growth_rate.pdf}
    \includegraphics{../sims/asymmetric/plots/fixed/extinct_rate_scaling.pdf}
    \includegraphics{../sims/asymmetric/plots/fixed/exp_avg_avg_strat_phe_0.pdf}
    \includegraphics{../sims/asymmetric/plots/fixed/avg_avg_strat_phe_0.pdf}
\end{figure*}

The results above motivate the search for a heuristic approximation of the distribution $p(\overline{s})$, constructed solely from properties of fixed strategies, that can predict the evolutionary outcome $\langle\overline{s}\rangle(A)$ as a function of $N_\text{ini}$ and $p_\text{mut}$.

To this end, it is useful to examine how $\langle\mu\rangle$ and $r_\text{ext}$ depend on the population size, as shown in the top panels of \autoref{fig:fixed_results_and_heuristic_approx}. We find that the mean growth rate depends only weakly on $N_\text{ini}$, except for very small populations. In contrast, the extinction rate exhibits a strong dependence, decreasing approximately as a power law, $r_\text{ext}\sim N_\text{ini}^{-\alpha}$. This scaling arises because extinctions are primarily driven by unfavorable environmental episodes: when the environment remains adverse for sufficiently long, the population decays exponentially from its typical size to extinction. Since the duration of environmental states is also exponentially distributed, this mechanism naturally leads to a power-law dependence of the extinction rate on $N_\text{ini}$.

We therefore propose the following heuristic form for the distribution of strategies:
\begin{equation} \label{eq:heuristic_distribution}
    p(\overline{s})\propto\frac{\exp(N_\text{ini}\cdot\langle\mu\rangle(\overline{s}))}{r_\text{ext}(N_\text{ini},\overline{s})+p_\text{mut}\cdot\langle\mu_b\rangle(\overline{s})},
\end{equation}
where $\langle\mu_b\rangle$ denotes the average birth rate \footnote{That is, the contribution to the growth rate arising from replication events only, excluding deaths.} and all quantities are measured in fixed-strategy simulations \footnote{To evaluate $\langle\mu\rangle$, $\langle\mu_b\rangle$ and $r_\text{ext}$ for arbitrary values of $\overline{s}$ and $N_\text{ini}$ we use spline interpolations of the numerical data, shown as dashed lines in \autoref{fig:fixed_results_and_heuristic_approx}.}.

The logic behind \autoref{eq:heuristic_distribution} is as follows. The numerator represents the probability that a given strategy is selected during the initial competitive phase following reinitialization, when $N_\text{ini}$ independently drawn strategies compete and small differences in growth are exponentially amplified. The denominator, in turn, accounts for the rate at which a strategy is lost or replaced. The term $r_\text{ext}^{-1}$ corresponds to the typical survival time of a population with that strategy, while the additional contribution proportional to $p_\text{mut}$ represents the rate at which the strategy is replaced by a new one due to mutation and subsequent fixation. Together, these contributions estimate the fraction of time the system spends in states effectively characterized by a single dominant strategy $\overline{s}$.

A key feature of \autoref{eq:heuristic_distribution} is the competition between two terms with fundamentally different scaling behaviors. The growth contribution enters exponentially through $\exp(N_\text{ini}\cdot\langle\mu\rangle(\overline{s}))$, while extinction enters algebraically through $r_\text{ext}$, which follows a power-law dependence on $N_\text{ini}$. This difference in scaling is crucial: it allows extinction risk to dominate at small population sizes, favoring safer strategies, while ensuring that growth maximization prevails in the large-$N_\text{ini}$ limit.

Despite its simplicity, this heuristic captures the transition from the growth-maximizing strategy to safer ones as $N_\text{ini}$ decreases, as well as its dependence on $p_\text{mut}$ \footnote{Notably, for large $N_\text{ini}$ the dependence on $p_\text{mut}$ disappears, confirming that, especially in this regime, the evolutionary outcome is controlled primarily by growth rather than the details of mutation.}. Additional results, including the full distributions, are provided in \autoref{sec:supplementary_results}.

\section{Discussion and outlook} \label{sec:discussion}

Our results provide a clear and minimal illustration of how evolutionary outcomes in finite populations cannot be inferred solely from the average instantaneous growth rate. The long-term dominant strategy is shaped not only by the mean behavior of the population but also by fluctuations, particularly those that can lead to extinction. In fluctuating environments with a population size limit, extinction is inevitable over long times and thus acts as a genuine selective pressure.

In the large-$N_\text{ini}$ limit, extinction risk becomes negligible, and evolution correctly selects the strategy that maximizes $\langle\mu\rangle$. Importantly, this growth-maximizing strategy is neither $s(A)=0$ nor $s(A)=1$, highlighting that maintaining phenotypic diversity can be advantageous in fluctuating environments. For smaller populations, however, extinction risk reshapes the adaptive landscape, biasing evolution toward safer, lower-growth strategies. This crossover demonstrates how demographic constraints fundamentally alter evolutionary optimization.

The heuristic approximation introduced here captures the qualitative competition between growth and extinction, clarifying the scaling mechanisms underlying the transition between growth-maximizing and safe strategies. Quantitative discrepancies remain, emphasizing that evolutionary dynamics cannot be fully reduced to fixed-strategy observables. Extending this framework toward a more systematic coarse-grained theory, possibly linked to quasi-stationary distributions, represents an important avenue for future work.

More broadly, this study provides a flexible framework to explore evolution in fluctuating environments. The model can be generalized to more environments or phenotypes, and to strategies that respond to environmental cues (sensing), enabling the systematic study of bet-hedging, plasticity, and stochastic selection in complex settings.

\printbibliography

\appendix

\section{The Gillespie algorithm and time series analysis} \label{sec:gillespie_algorithm}

\bigskip
\textcolor{red}{FINISHED UP TO HERE}
\bigskip

\section{Different model parameters} \label{sec:symmetric_parameters}

\section{Supplementary Results} \label{sec:supplementary_results}

\begin{figure}
    \centering
    \caption{\dots} \label{fig:evidence_of_common_strategy}
    \includegraphics{../sims/asymmetric/plots/time_series/avg_strat_phe_0.pdf}
    \includegraphics{../sims/asymmetric/plots/prob_mut/avg_growth_rate.pdf}
\end{figure}

Time series data confirm that, in the low-mutation regime, the population is effectively characterized by a common strategy at each instant (\autoref{fig:evidence_of_common_strategy}). The apparent breadth of the long-term strategy distribution arises from averaging over time and across realizations, not from coexistence of widely different strategies at a single moment.

ADD PLOTS OF DISTRIBUTIONS.

\section{Incremental mutations} \label{sec:incremental_mutations}

\begin{figure}
    \centering
    \caption{\dots} \label{fig:incremental_mutations}
    \includegraphics{../sims/incremental/plots/strat_phe_0_i/avg_avg_strat_phe_0.pdf}
    \includegraphics{../sims/incremental/plots/n_agents_i/avg_avg_strat_phe_0.pdf}
\end{figure}

% Possible Journals:
% 1. Proceedings of the National Academy of Sciences
% 2. Physical Review E
% 3. Journal of Theoretical Biology

\end{document}
